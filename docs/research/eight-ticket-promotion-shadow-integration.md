# 実戦8点・候補Aの検証専用接続

2026-09-25。対象は承認済みの接続・副作用テスト。本番採用ではない。

## 接続

既存の `js/frame-rise-fall-replay-preload.js` からサーバー専用preloadを読み込む。対象は `practical-priority-shadow` の比較出力のみ。既存の順位候補研究の出力・指紋は保持し、独立した `eightTicketPromotionShadow` を追加する。ブラウザ側のAPI、選定エンジン、実戦券、購入判断、note生成は変更しない。

保存場所は日別予想行の `practicalPriorityShadow.eightTicketPromotionShadow`。既存collectorが付けるcapturedAtとsourceCommitを継承する。選定が終わった時点の8点と、A案の8点を分けて保存する。A案は既存の候補補完で90点以上として採用した券が3コース固定救済で上書きされた場合だけ元券を戻す。元券は履歴から取り、1-2-5等を固定しない。元券・位置・重複・救済版・救済カテゴリの整合性が確認できない時は変更候補を作らない。

## 記録と集計

2026-09-25以降に実際に保存された比較だけを対象にする。作成時刻が保存時刻より前、締切以降、入力条件が締切前契約を満たさない、現在の保存券と比較時の元券が違う場合は除外。過去287R等から後付けして前向き件数に数えない。

既存の継続成績workflowで従来台帳を保存した後、別reportを生成してActions artifactへ保存する。研究レポートの失敗で既存台帳保存を止めない。全8点観測数、A発動数、確定発動数、追加的中、失った的中、購入額、払戻、収支、世代別内訳を区別する。未決と不成立・返還・不正な結果を分ける。本人の実購入成績ではなく、各券100円仮定の検証。

## 範囲と制限

これは通常の予想収集経路への接続。別のnote専用経路や全レースの捕捉を保証しない。新規の発動件数は記録が揃うまで未確認。未知レースでの有効性・本番採用は別判断。自動採用は常に無効。

保存済みの入力を現在のエンジンに入れ直すと、当時と異なる券になる場合がある。保存券の遡及成績を再生成版で置き換えず、接続テストと性能検証を分離する。

## 検証

`node --test tests/eight-ticket-promotion-shadow.test.cjs` と既存の実戦厳選・固定救済・順位shadow・収集保存・note・継続台帳の回帰を実行する。前段で実行した42件は隔離接続の試験であり、このブランチのCI実績は別途記録する。

戻す場合はpreloadの追加読込とworkflowの別report生成だけを外す。過去予想、結果、成績台帳は変更・削除しない。
