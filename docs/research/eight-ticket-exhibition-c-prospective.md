# 実戦8点 C案：前向き比較への接続（2026-09-26）

本番採用ではない。元の固定救済Aと別研究。アプリ・note・実戦券・購入判断・通常成績・予想世代は変更しない。

## 固定した選択条件
強い逃げの別頭整理後の実戦8点。候補補完が同じ1着・2着軸へ2券以上集中した時だけ、最後の補完1券を、別の2着艇の同点・90点以上の保存候補へ交換する。採用済みの1/2着軸が少ない順、同数は券の文字列順。6艇の公式進入・非負の展示ST・展示タイムを必須とし、1号艇が公式1コースであることを確認する。本線・フォーメーション・独立展開を保護し、8点を維持する。STと展示タイムの両優位を要求するD案は接続しない。1-4-2の固定追加ではない。

前回の厳格版Cと同じ新形式プールの根拠を必須とする。候補のticketSelected=false、構造化された3着順の艇/役割、同点を検査する。旧保存形式の158Rを扱った感度分析の代替入力は、ライブ比較には持ち込まない。欠損を推測や他日の情報で補わない。

## 保存・集計
既存collect-predictionsの完成したレコードにだけ、eightTicketExhibitionShadowを追加する。選定器は呼び直さない。通常予想をnote用にコンパクト化し直す経路でも、その完成後に同じ任意helperを使う。別の収集cronや新しいブラウザ経路は作らない。

実行時刻・保存時刻・締切・raceKey・sourceCommit・固定したsourceGeneration・入力指紋を保存する。結果/オッズを含まない入力投影と実装指紋で再現照合し、時刻不正・改ざん・世代違い・旧レコードは除外。過去174R、発想の起点の徳山3R、A記録はCの新規成績に混ぜない。生成障害はunavailableを記録し、通常の収集を止めない。

既存の継続台帳workflow内でCだけのreportを生成し、別artifactへ保存する。Aレポート・通常台帳は上書きしない。生成途中の欠損はcomplete=falseと失敗で示す。公式結果の確認、返還/F/L等の除外はAに準じ、観測数・発動数・確定発動数・追加的中・失った的中を区別する。購入額・回収率は各券100円仮定で本人実購入ではない。未生成のsnapshotを後付けしない。

## 判断と検証
自動採用は常に無効、採用状態はNOT_APPROVED。参考比較の1件780円を将来性能の証明とはしない。条件を今後の的中/不的中に合わせて変更せず、必要な変更は別バージョンとして扱う。テスト件数、main反映、実収集での初回保存、実レースの効果判定を分けて報告する。

node --require ./js/frame-rise-fall-replay-preload.js --test tests/eight-ticket-exhibition-shadow.test.cjs

戻す場合はcollectorの任意helper呼出とworkflowのCステップだけ外す。保存済みの予想・結果・研究記録は削除しない。
