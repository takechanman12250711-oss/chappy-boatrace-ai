"use strict";

function normalizeTicket(value) { const boats=String(value||"").match(/[1-6]/g)||[]; return boats.length>=3?boats.slice(0,3).join("-"):""; }
function normalizeCategory(value){const text=String(value||"");if(/本線|本命|中心/.test(text))return"本線";if(/押さえ|安全/.test(text))return"押さえ";if(/流し/.test(text))return"流し";if(/独立展開|展開追加/.test(text))return"独立展開";if(/万舟|穴|高配当/.test(text))return"万舟・穴";return"その他";}
function validCourse(value){const course=Number(value);return Number.isInteger(course)&&course>=1&&course<=6;}
function branchMap(prediction){const branches=prediction?.aiCore?.formations?.evidence?.branches||prediction?.formations?.evidence?.branches||[];return new Map((Array.isArray(branches)?branches:[]).map(branch=>[String(branch?.id||""),branch]).filter(([id])=>id));}
function branchUsesCourseEvidence(branch){if(!branch||branch.kind!=="independent-scenario")return false;const phase=branch.phaseEvidence||{};const checks=Array.isArray(branch.evidenceChecks)?branch.evidenceChecks:[];const explicitCheck=checks.some(check=>{const key=String(check?.key||"").toLowerCase();const source=String(check?.source||"").toLowerCase();const role=String(check?.role||"").toLowerCase();return role==="course"||key.includes("course")||source.includes("coursebyboat");});if(explicitCheck)return true;if(phase.kind==="alternate-head"&&validCourse(phase?.attack?.course))return true;if(phase.kind==="hold-continuation"){if(validCourse(phase?.target?.course))return true;if(String(phase?.partner?.type||"")==="inside")return true;}return false;}
function courseClaimForTicket(prediction,evidenceRow){const ids=Array.isArray(evidenceRow?.branchIds)?evidenceRow.branchIds:[];if(!ids.length)return null;const branches=branchMap(prediction);if(!ids.map(id=>branches.get(String(id))).some(branchUsesCourseEvidence))return null;return{theoryKey:"course",label:"コース理論",theoryVersion:"structured-course-validation-v1",formal:true,source:"structured-course-validation"};}
function supportStatements(support){const confirms=Array.isArray(support?.confirms)?support.confirms:Array.isArray(support?.confirmations)?support.confirmations:[];const alerts=Array.isArray(support?.alerts)?support.alerts:Array.isArray(support?.cautions)?support.cautions:[];return[...confirms,...alerts].map(String);}
function supportAttackBoatNo(prediction,support){return Number(support?.attackBoatNo||support?.centerBoatNo||prediction?.flowPriority?.attackBoatNo||prediction?.flowPriority?.attackBoat||0);}
function stSlitEvidence(prediction){const support=prediction?.flowSupport||prediction?.stExhibitionSupport||{};const attackBoatNo=supportAttackBoatNo(prediction,support);const stCoverage=Number(support?.dataCoverage?.st||0);const stRank=Number(support?.attackSTRank||0);const statements=supportStatements(support);const explicit=statements.some(text=>/ST|スリット/.test(text));return{formal:attackBoatNo>=1&&attackBoatNo<=6&&stCoverage>=4&&stRank>=1&&stRank<=6&&explicit,attackBoatNo,stCoverage,stRank,statements};}
function stSlitClaimForTicket(prediction,ticket){const evidence=stSlitEvidence(prediction);if(!evidence.formal)return null;const boats=normalizeTicket(ticket).split("-").map(Number);if(!boats.includes(evidence.attackBoatNo))return null;return{theoryKey:"stSlit",label:"ST・スリット理論",theoryVersion:"flow-support-st-slit-v1",formal:true,source:"flow-support-st-slit"};}
function exhibitionFootEvidence(prediction){const support=prediction?.flowSupport||prediction?.stExhibitionSupport||{};const attackBoatNo=supportAttackBoatNo(prediction,support);const exhibitionCoverage=Number(support?.dataCoverage?.exhibition||0);const exhibitionRank=Number(support?.attackExhibitionRank||0);const statements=supportStatements(support);const explicit=statements.some(text=>/展示|足|気配/.test(text));return{formal:attackBoatNo>=1&&attackBoatNo<=6&&exhibitionCoverage>=4&&exhibitionRank>=1&&exhibitionRank<=6&&explicit,attackBoatNo,exhibitionCoverage,exhibitionRank,statements};}
function exhibitionFootClaimForTicket(prediction,ticket){const evidence=exhibitionFootEvidence(prediction);if(!evidence.formal)return null;const boats=normalizeTicket(ticket).split("-").map(Number);if(!boats.includes(evidence.attackBoatNo))return null;return{theoryKey:"exhibitionFoot",label:"展示・足理論",theoryVersion:"flow-support-exhibition-foot-v1",formal:true,source:"flow-support-exhibition-foot"};}
function localWaterEvidence(prediction){const support=prediction?.venueWaterSupport||{};const venue=String(support?.venue||"").trim();const wind=Number(support?.wind);const wave=Number(support?.wave);const tide=String(support?.tide||"").trim();const statements=supportStatements(support);const hasMeasuredCondition=Number.isFinite(wind)||Number.isFinite(wave)||Boolean(tide);const hasSpecificVenueRule=statements.some(text=>!/開催場の水面特性を補助評価/.test(text)&&/イン|差し|潮|風|波|水面|ナイター|展示|乗り心地/.test(text));return{formal:Boolean(venue)&&statements.length>0&&(hasMeasuredCondition||hasSpecificVenueRule),venue,wind:Number.isFinite(wind)?wind:null,wave:Number.isFinite(wave)?wave:null,tide,statements};}
function localWaterClaimForTicket(prediction,ticket){const evidence=localWaterEvidence(prediction);if(!evidence.formal||!normalizeTicket(ticket))return null;return{theoryKey:"localWater",label:"当地・水面理論",theoryVersion:"venue-water-support-v1",formal:true,source:"venue-water-support"};}
function skillEvidence(prediction){const support=prediction?.skillLocalSupport||{};const attackBoatNo=supportAttackBoatNo(prediction,support);const target=(Array.isArray(support?.boats)?support.boats:[]).find(row=>Number(row?.boatNo)===attackBoatNo)||null;const statements=supportStatements(support).filter(text=>!/当地/.test(text));const explicit=statements.some(text=>/A1級|A2級|B2級|技量|全国勝率|平均ST|1着率/.test(text));const hasSkillData=Boolean(target&&(target.grade||target.nationalWinRate!==null||target.avgST!==null||target.firstRate!==null));return{formal:attackBoatNo>=1&&attackBoatNo<=6&&hasSkillData&&explicit,attackBoatNo,target,statements};}
function skillClaimForTicket(prediction,ticket){const evidence=skillEvidence(prediction);if(!evidence.formal)return null;const boats=normalizeTicket(ticket).split("-").map(Number);if(!boats.includes(evidence.attackBoatNo))return null;return{theoryKey:"skill",label:"技量理論",theoryVersion:"skill-local-support-v1",formal:true,source:"skill-local-support"};}
function motorEvidence(prediction){const support=prediction?.motorEngineSupport||{};const centerBoatNo=supportAttackBoatNo(prediction,support);const rate=Number(support?.centerMotorRate);const statements=supportStatements(support);const explicit=statements.some(text=>/モーター実績(上位|下位)/.test(text));const normalMode=support?.newEngineMode===false||String(support?.mode||"")==="normal";return{formal:centerBoatNo>=1&&centerBoatNo<=6&&normalMode&&Number.isFinite(rate)&&explicit,centerBoatNo,centerMotorRate:Number.isFinite(rate)?rate:null,mode:String(support?.mode||""),statements};}
function motorClaimForTicket(prediction,ticket){const evidence=motorEvidence(prediction);if(!evidence.formal)return null;const boats=normalizeTicket(ticket).split("-").map(Number);if(!boats.includes(evidence.centerBoatNo))return null;return{theoryKey:"motor",label:"モーター理論",theoryVersion:"motor-engine-support-v1",formal:true,source:"motor-engine-support"};}
function wallEvidence(prediction){const wall=prediction?.aiCore?.wallTheory||prediction?.wallTheory||prediction?.raceScenarios?.wallTheory||{};const attackerNo=Number(wall?.attackerNo||prediction?.aiCore?.raceScenarios?.attacker||0);const wallCandidateNo=Number(wall?.wallCandidateNo||0);const wallBoat=Number(wall?.wallBoat||0);const state=String(wall?.state||"").trim();const score=Number(wall?.score);const grade=String(wall?.grade||"").trim();const formalState=/^(壁成立|互角|壁崩れ)$/.test(state);const hasStructure=attackerNo>=1&&attackerNo<=6&&wallCandidateNo>=1&&wallCandidateNo<=6&&Number.isFinite(score)&&Boolean(grade);return{formal:formalState&&hasStructure,attackerNo,wallCandidateNo,wallBoat:wallBoat>=1&&wallBoat<=6?wallBoat:null,state,score:Number.isFinite(score)?score:null,grade};}
function wallClaimForTicket(prediction,ticket){const evidence=wallEvidence(prediction);if(!evidence.formal)return null;const boats=normalizeTicket(ticket).split("-").map(Number);if(!boats.includes(evidence.attackerNo))return null;return{theoryKey:"wallBoat",label:"壁艇理論",theoryVersion:"ai-core-wall-theory-v1",formal:true,source:"ai-core-wall-theory-v1"};}
function theoryClaimsFrom(prediction,practicalTickets){const evidence=prediction?.practicalSelection?.verificationEvidence||prediction?.verificationEvidence||{};const evidenceByTicket=new Map((Array.isArray(evidence?.tickets)?evidence.tickets:[]).map(row=>[normalizeTicket(row?.ticket),row]));return(Array.isArray(practicalTickets)?practicalTickets:[]).map(item=>{const row=typeof item==="string"?{ticket:item}:(item||{});const ticket=normalizeTicket(row.ticket||row.line||row.formation);const evidenceRow=evidenceByTicket.get(ticket)||{};const baseClaims=Array.isArray(evidenceRow.theoryClaims)?evidenceRow.theoryClaims:(Array.isArray(row.theoryClaims)?row.theoryClaims:[]);const extra=[courseClaimForTicket(prediction,evidenceRow),stSlitClaimForTicket(prediction,ticket),exhibitionFootClaimForTicket(prediction,ticket),localWaterClaimForTicket(prediction,ticket),skillClaimForTicket(prediction,ticket),motorClaimForTicket(prediction,ticket),wallClaimForTicket(prediction,ticket)].filter(Boolean);const claims=[...baseClaims,...extra].filter((claim,index,all)=>{const key=String(claim?.theoryKey||claim?.key||"").trim();return key&&all.findIndex(other=>String(other?.theoryKey||other?.key||"").trim()===key)===index;});return{ticket,category:normalizeCategory(row.category||row.role||evidenceRow.category||(Array.isArray(evidenceRow.categories)?evidenceRow.categories[0]:"")),claims};}).filter(row=>row.ticket&&row.claims.length);}
function build(prediction,practicalTickets){const tickets=theoryClaimsFrom(prediction,practicalTickets);const groups=new Map();tickets.forEach(row=>{const seen=new Set();row.claims.forEach(claim=>{const key=String(claim?.theoryKey||claim?.key||"").trim();if(!key||seen.has(key))return;seen.add(key);const group=groups.get(key)||{theoryKey:key,label:String(claim?.label||claim?.theoryLabel||key).trim(),version:String(claim?.theoryVersion||claim?.version||"").trim(),formal:claim?.formal===true,sources:new Set(),ticketCount:0,mainTicketCount:0,categories:new Set(),tickets:[]};const source=String(claim?.source||"").trim();if(source)group.sources.add(source);group.ticketCount+=1;if(row.category==="本線")group.mainTicketCount+=1;group.categories.add(row.category);group.tickets.push(row.ticket);groups.set(key,group);});});const theories=[...groups.values()].map(group=>({theoryKey:group.theoryKey,label:group.label,version:group.version,formal:group.formal,sources:[...group.sources].sort(),ticketCount:group.ticketCount,mainTicketCount:group.mainTicketCount,categories:[...group.categories],tickets:[...new Set(group.tickets)]})).sort((a,b)=>b.mainTicketCount-a.mainTicketCount||b.ticketCount-a.ticketCount||a.theoryKey.localeCompare(b.theoryKey));return{schemaVersion:1,status:theories.length?"tracked":"no-formal-theory-claims",theoryCount:theories.length,ticketCount:tickets.length,theories,usableForPrediction:false,automaticApplication:false};}
module.exports={build,theoryClaimsFrom,branchUsesCourseEvidence,courseClaimForTicket,stSlitEvidence,stSlitClaimForTicket,exhibitionFootEvidence,exhibitionFootClaimForTicket,localWaterEvidence,localWaterClaimForTicket,skillEvidence,skillClaimForTicket,motorEvidence,motorClaimForTicket,wallEvidence,wallClaimForTicket};
