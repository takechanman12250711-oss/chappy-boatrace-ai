name: Diagnose review mode in WebKit

on:
  pull_request:
    paths:
      - ".github/workflows/diagnose-review-mode-webkit.yml"
      - "scripts/diagnose-review-mode-webkit.mjs"
      - "index.html"
      - "js/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  webkit:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout branch
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Playwright WebKit
        run: |
          npm install --no-save --no-package-lock playwright@1.55.0
          npx playwright install --with-deps webkit

      - name: Start local static server
        run: |
          python3 -m http.server 4173 > /tmp/chappy-http.log 2>&1 &
          echo $! > /tmp/chappy-http.pid
          for attempt in $(seq 1 30); do
            if curl --fail --silent http://127.0.0.1:4173/ >/dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat /tmp/chappy-http.log
          exit 1

      - name: Reproduce review mode on branch in WebKit
        env:
          APP_URL: http://127.0.0.1:4173/
          EXPECTED_RUNTIME: 20260825-mobile-startup-terminal1
          REVIEW_DATE: "2026-08-24"
          REVIEW_PLACE: 蒲郡
          REVIEW_RACE: 12R
          DIAG_OUTPUT: artifacts/local
        run: node scripts/diagnose-review-mode-webkit.mjs

      - name: Reproduce review mode on production in WebKit
        if: always()
        continue-on-error: true
        env:
          APP_URL: https://takechanman12250711-oss.github.io/chappy-boatrace-ai/
          EXPECTED_RUNTIME: 20260824-readonly-core-fix1
          REVIEW_DATE: "2026-08-24"
          REVIEW_PLACE: 蒲郡
          REVIEW_RACE: 12R
          DIAG_OUTPUT: artifacts/production
        run: node scripts/diagnose-review-mode-webkit.mjs

      - name: Show HTTP server log
        if: always()
        run: cat /tmp/chappy-http.log || true

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: review-mode-webkit-diagnostics
          path: artifacts
          if-no-files-found: warn
          retention-days: 7
