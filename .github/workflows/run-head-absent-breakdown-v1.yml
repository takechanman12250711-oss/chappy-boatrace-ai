name: Run head absent breakdown v1
on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'scripts/analyze-head-absent-breakdown-v1.cjs'
      - '.github/workflows/run-head-absent-breakdown-v1.yml'
permissions:
  contents: read
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Run head-absent breakdown
        run: node scripts/analyze-head-absent-breakdown-v1.cjs > head-absent-breakdown-v1.json
      - name: Validate research boundary and report
        run: |
          node - <<'NODE'
          const fs=require('fs');const r=JSON.parse(fs.readFileSync('head-absent-breakdown-v1.json','utf8'));
          if(r.productionChanged!==false||r.automaticApplication!==false||r.usableForPrediction!==false) process.exit(1);
          if(r.targetHeadAbsentCount<=0) process.exit(1);
          console.log(JSON.stringify({target:r.targetHeadAbsentCount,winnerLane:r.winnerLane,signals:r.signals,bestPosition:r.bestPosition,bestScoreBand:r.bestScoreBand},null,2));
          NODE
      - uses: actions/upload-artifact@v4
        with:
          name: head-absent-breakdown-v1
          path: head-absent-breakdown-v1.json
          retention-days: 90
