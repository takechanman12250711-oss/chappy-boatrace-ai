name: Apply theory shadow A/B

on:
  pull_request:
    paths:
      - "js/theory-shadow-ab.js"
      - "scripts/test-theory-shadow-ab.js"
      - "scripts/apply-theory-shadow-ab.js"
      - ".github/workflows/apply-theory-shadow-ab.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply integration
        run: node scripts/apply-theory-shadow-ab.js
      - name: Verify
        run: |
          node --check js/theory-shadow-ab.js
          node --check scripts/collect-predictions.js
          node scripts/test-theory-shadow-ab.js
          node scripts/check-charter.js
      - name: Commit applied source
        run: |
          if git diff --quiet -- scripts/collect-predictions.js; then
            echo "No source change required"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/collect-predictions.js
          git commit -m "Store theory shadow A/B snapshots"
          git push
