name: Connect venue frame reference

on:
  push:
    branches:
      - main
    paths:
      - "js/venue-frame-reference.js"
      - "scripts/connect-venue-frame-reference.js"
      - ".github/workflows/connect-venue-frame-reference.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: connect-venue-frame-reference
  cancel-in-progress: false

jobs:
  connect:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Connect script
        run: node scripts/connect-venue-frame-reference.js

      - name: Commit connection when changed
        run: |
          if git diff --quiet -- index.html; then
            echo "No index.html changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Connect venue frame reference"
          git pull --rebase origin main
          git push origin main
