name: Check fixed5 pair reallocation 3-1
on:
  pull_request:
    paths:
      - 'scripts/ab-fixed5-pair-reallocation-31.js'
      - '.github/workflows/check-fixed5-pair-reallocation-31.yml'
jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/restore-daily-prediction-source.js --all
      - run: node --check scripts/ab-fixed5-pair-reallocation-31.js
      - run: node scripts/ab-fixed5-pair-reallocation-31.js
