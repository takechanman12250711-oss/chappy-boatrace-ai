name: Check head miss scenario gap audit
on:
  pull_request:
    paths:
      - 'scripts/analyze-head-miss-scenario-gap-audit.js'
      - '.github/workflows/check-head-miss-scenario-gap-audit.yml'
  workflow_dispatch:
jobs:
  analyze:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/restore-daily-prediction-source.js --all
      - run: node --check scripts/analyze-head-miss-scenario-gap-audit.js
      - run: node scripts/analyze-head-miss-scenario-gap-audit.js
