name: Check race flow branch profit
on:
  pull_request:
    paths:
      - "scripts/build-race-flow-branch-profit-report.js"
      - "scripts/test-race-flow-branch-profit-report.js"
      - ".github/workflows/check-race-flow-branch-profit.yml"
  push:
    branches: [main]
    paths:
      - "scripts/build-race-flow-branch-profit-report.js"
      - "scripts/collect-predictions.js"
      - ".github/workflows/check-race-flow-branch-profit.yml"
  workflow_run:
    workflows:
      - "Collect automatic race predictions"
      - "Collect official race results"
    types: [completed]
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: chappy-main-data-writers
  cancel-in-progress: false
jobs:
  verify:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || 'main' }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Verify race flow analyzer
        run: |
          node --check scripts/build-race-flow-branch-profit-report.js
          node scripts/test-race-flow-branch-profit-report.js
      - name: Restore canonical sources and build report
        if: github.event_name != 'pull_request'
        run: |
          node scripts/restore-daily-prediction-source.js --all
          node scripts/build-race-flow-branch-profit-report.js
          node scripts/prepare-daily-prediction-git-save.js --all
      - name: Save race flow branch report
        if: github.event_name != 'pull_request'
        run: |
          git add data/stats/race-flow-branch-profit-report.json
          if git diff --cached --quiet; then
            echo "race-flow report unchanged"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Update race flow branch profit report"
          git pull --rebase origin main
          git push origin main
