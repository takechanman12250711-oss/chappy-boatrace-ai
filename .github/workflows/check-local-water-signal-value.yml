name: Check local water signal value
on:
  pull_request:
    paths:
      - 'scripts/analyze-local-water-signal-value.js'
      - '.github/workflows/check-local-water-signal-value.yml'
  workflow_dispatch:
jobs:
  analysis:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 1 }
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: node scripts/restore-daily-prediction-source.js --all
      - run: node --check scripts/analyze-local-water-signal-value.js
      - run: node scripts/analyze-local-water-signal-value.js
