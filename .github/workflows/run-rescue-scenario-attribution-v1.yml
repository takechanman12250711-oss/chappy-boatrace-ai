name: Run rescue scenario attribution v1

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'scripts/analyze-rescue-scenario-attribution-v1.cjs'
      - '.github/workflows/run-rescue-scenario-attribution-v1.yml'

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Run rescue scenario attribution
        run: node scripts/analyze-rescue-scenario-attribution-v1.cjs > rescue-scenario-attribution-v1.json
      - name: Validate non-production audit
        run: |
          node - <<'NODE'
          const fs = require('fs');
          const r = JSON.parse(fs.readFileSync('rescue-scenario-attribution-v1.json','utf8'));
          if (r.productionChanged !== false || r.automaticApplication !== false || r.usableForPrediction !== false) process.exit(1);
          console.log(JSON.stringify(r.totals, null, 2));
          NODE
      - uses: actions/upload-artifact@v4
        with:
          name: rescue-scenario-attribution-v1
          path: rescue-scenario-attribution-v1.json
          retention-days: 90
