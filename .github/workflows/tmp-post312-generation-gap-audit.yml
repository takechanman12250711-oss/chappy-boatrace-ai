name: Audit post-312 generation gaps
on:
  workflow_dispatch:
jobs:
  audit-summary:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR #313 analysis complete; temporary audit only; do not merge."
