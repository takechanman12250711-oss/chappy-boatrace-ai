name: Check local water branch
on:
  pull_request:
    paths:
      - "scripts/build-local-water-branch-report.js"
      - "scripts/test-local-water-branch-report.js"
      - "scripts/test-local-water-branch-writer-contract.js"
      - ".github/workflows/check-local-water-branch.yml"
      - ".github/workflows/refresh-local-water-branch-report.yml"
      - ".github/workflows/collect-results.yml"
  push:
    branches: [main]
    paths:
      - "scripts/build-local-water-branch-report.js"
      - "scripts/test-local-water-branch-report.js"
      - "scripts/test-local-water-branch-writer-contract.js"
      - ".github/workflows/check-local-water-branch.yml"
      - ".github/workflows/refresh-local-water-branch-report.yml"
      - ".github/workflows/collect-results.yml"
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || 'main' }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: |
          node --check scripts/build-local-water-branch-report.js
          node scripts/test-local-water-branch-report.js
          node scripts/test-local-water-branch-writer-contract.js
      - name: Build read-only real diagnostics
        if: github.event_name != 'pull_request'
        run: |
          node scripts/restore-daily-prediction-source.js --all
          node scripts/build-local-water-branch-report.js
