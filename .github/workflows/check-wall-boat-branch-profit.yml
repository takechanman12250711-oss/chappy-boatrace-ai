name: Check wall boat branch profit
on:
  pull_request:
    paths:
      - "scripts/build-wall-boat-branch-profit-report.js"
      - "scripts/test-wall-boat-branch-profit-report.js"
      - "scripts/test-breakdown-writer-contract.js"
      - ".github/workflows/collect-results.yml"
      - ".github/workflows/check-wall-boat-branch-profit.yml"
  push:
    branches: [main]
    paths:
      - "scripts/build-wall-boat-branch-profit-report.js"
      - "scripts/collect-predictions.js"
      - "js/theory-tag-snapshot.js"
      - ".github/workflows/check-wall-boat-branch-profit.yml"
  workflow_run:
    workflows:
      - "Collect automatic race predictions"
      - "Collect official race results"
    types: [completed]
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  verify:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || 'main' }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Verify wall boat analyzer
        run: |
          node --check scripts/build-wall-boat-branch-profit-report.js
          node scripts/test-wall-boat-branch-profit-report.js
          node scripts/test-breakdown-writer-contract.js
      - name: Restore canonical sources and build report
        if: github.event_name != 'pull_request'
        run: |
          node scripts/restore-daily-prediction-source.js --all
          node scripts/build-wall-boat-branch-profit-report.js
          git diff --check
