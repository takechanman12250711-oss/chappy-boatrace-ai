name: Run second-place rank9 rank12 stress v1
on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'scripts/analyze-second-place-rank9-rank12-stress-v1.cjs'
      - '.github/workflows/run-second-place-rank9-rank12-stress-v1.yml'
permissions:
  contents: read
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Run second-place stress audit
        run: node scripts/analyze-second-place-rank9-rank12-stress-v1.cjs > second-place-rank9-rank12-stress-v1.json
      - name: Validate research-only boundary
        run: |
          node - <<'NODE'
          const fs=require('fs');const r=JSON.parse(fs.readFileSync('second-place-rank9-rank12-stress-v1.json','utf8'));
          if(r.productionChanged!==false||r.automaticApplication!==false||r.usableForPrediction!==false) process.exit(1);
          console.log(JSON.stringify(r.strategies,null,2));
          NODE
      - uses: actions/upload-artifact@v4
        with:
          name: second-place-rank9-rank12-stress-v1
          path: second-place-rank9-rank12-stress-v1.json
          retention-days: 90
