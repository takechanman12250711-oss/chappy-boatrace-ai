name: Check sashi overcall to escape
on:
  pull_request:
    paths:
      - 'scripts/analyze-sashi-overcall-to-escape.js'
      - '.github/workflows/check-sashi-overcall-to-escape.yml'
  workflow_dispatch:
jobs:
  analysis:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 1 }
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: node scripts/restore-daily-prediction-source.js --all
      - run: node --check scripts/analyze-sashi-overcall-to-escape.js
      - run: node scripts/analyze-sashi-overcall-to-escape.js
