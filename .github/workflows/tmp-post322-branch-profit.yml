name: Audit post-322 branch profit
on:
  pull_request:
    branches: [main]
    paths:
      - 'scripts/tmp-audit-branch-profit.js'
      - '.github/workflows/tmp-post322-branch-profit.yml'
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/tmp-audit-branch-profit.js
      - uses: actions/upload-artifact@v4
        with:
          name: post322-branch-profit
          path: tmp-analysis-output/post322-branch-profit.json
