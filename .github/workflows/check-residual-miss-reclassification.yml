name: Check residual miss reclassification
on:
  pull_request:
    paths:
      - 'scripts/analyze-residual-miss-reclassification.js'
      - '.github/workflows/check-residual-miss-reclassification.yml'
  workflow_dispatch:
jobs:
  analyze:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/restore-daily-prediction-source.js --all
      - run: node --check scripts/analyze-residual-miss-reclassification.js
      - run: node scripts/analyze-residual-miss-reclassification.js
