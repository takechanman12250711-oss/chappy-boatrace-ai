name: Check sashi safety loss patterns
on:
  pull_request:
    paths:
      - 'scripts/analyze-sashi-safety-loss-patterns.js'
      - '.github/workflows/check-sashi-safety-loss-patterns.yml'
jobs:
  analysis:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 1 }
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: node scripts/restore-daily-prediction-source.js --all
      - run: node --check scripts/analyze-sashi-safety-loss-patterns.js
      - run: node scripts/analyze-sashi-safety-loss-patterns.js
