"use strict";
const fs=require("node:fs");
const file="js/practical-selection.js";
let s=fs.readFileSync(file,"utf8");
function replaceOnce(from,to){if(!s.includes(from))throw new Error(`anchor not found: ${from.slice(0,80)}`);s=s.replace(from,to);}
replaceOnce("  const MINIMUM_FLOW_ROLE_SCORE = 65;\n",`  const MINIMUM_FLOW_ROLE_SCORE = 65;\n  const MINIMUM_CANDIDATE_PROMOTION_SCORE =\n    90;\n`);
replaceOnce(`    expansionCandidates.forEach(\n      row => {\n        addExpansion(row);\n      }\n    );\n\n    candidates.forEach(`,`    expansionCandidates.forEach(\n      row => {\n        addExpansion(row);\n      }\n    );\n\n    /*\n      candidate-only は安全ゲートを外さない。\n      既存の通常枠・独立展開を確定した後、最大10点までの空き枠だけを使う。\n      1〜3着すべてに物理根拠があり、priority 90以上の候補だけを補完する。\n    */\n    function hasThreePositionPhysicalEvidence(row) {\n      const positions = new Set(\n        arrayify(row?.physicalCoverage)\n          .map(claim =>\n            Number(claim?.position || 0)\n          )\n          .filter(position =>\n            position >= 1 &&\n            position <= 3\n          )\n      );\n      return positions.size === 3;\n    }\n\n    const candidateOnlyPromotionPool =\n      candidates\n        .filter(({ row, validation }) => {\n          const candidateOnly =\n            (\n              validation.valid === false &&\n              validation.reasonCode ===\n                \"CANDIDATE_ONLY_EVALUATION\"\n            ) ||\n            (\n              validation.valid === true &&\n              validation.purchaseEligible === true &&\n              validation.expansionEligible === false\n            );\n\n          return (\n            candidateOnly &&\n            !used.has(row.ticket) &&\n            row.priorityScore >=\n              MINIMUM_CANDIDATE_PROMOTION_SCORE &&\n            hasThreePositionPhysicalEvidence(row)\n          );\n        })\n        .sort(\n          (a, b) =>\n            b.row.priorityScore -\n              a.row.priorityScore ||\n            a.row.ticket.localeCompare(\n              b.row.ticket\n            )\n        );\n\n    candidateOnlyPromotionPool.forEach(\n      ({ row }) => {\n        if (\n          selected.length >=\n          MAXIMUM_COUNT\n        ) {\n          return;\n        }\n\n        const promoted = {\n          ...row,\n          coverage: [\n            ...arrayify(\n              row.physicalCoverage\n            )\n          ],\n          category: \"候補補完\",\n          selectionTier: \"候補補完\",\n          candidatePromotion: true,\n          candidatePromotionThreshold:\n            MINIMUM_CANDIDATE_PROMOTION_SCORE,\n          candidatePromotionReason:\n            \"1〜3着すべてに物理根拠があり、priority 90以上のため空き枠へ補完\",\n          comment:\n            row.comment ||\n            row.scenarioSummary ||\n            \"3着まで物理根拠がそろう高優先度候補を最大10点の空き枠へ補完。\"\n        };\n\n        used.add(promoted.ticket);\n        selected.push(promoted);\n        recordDecision(\n          promoted,\n          true,\n          \"CANDIDATE_ONLY_PROMOTED\",\n          promoted.candidatePromotionReason\n        );\n      }\n    );\n\n    candidates.forEach(`);
replaceOnce(`    const expansionSummary = {\n      normalCount:\n        normalTicketCount,`, `    const candidatePromotionTickets =\n      finalizedTickets\n        .filter(\n          row =>\n            row.selectionTier ===\n            \"候補補完\"\n        )\n        .map(row => ({\n          ticket: row.ticket,\n          priorityScore:\n            row.priorityScore,\n          roleLabels: [\n            ...arrayify(\n              row.roleLabels\n            )\n          ]\n        }));\n    const expansionSummary = {\n      normalCount:\n        normalTicketCount,`);
replaceOnce(`      finalCount:\n        finalizedTickets.length,\n      hasIndependentAdditions:`, `      finalCount:\n        finalizedTickets.length,\n      candidatePromotionCount:\n        candidatePromotionTickets.length,\n      candidatePromotionThreshold:\n        MINIMUM_CANDIDATE_PROMOTION_SCORE,\n      candidatePromotionTickets,\n      hasIndependentAdditions:`);
replaceOnce(`      reason:\n        addedTickets.length\n          ? \"通常枠とは別に、時系列と艇・着順・役割が一致した独立展開だけを追加。\"\n          : \"購入可能な独立展開がないため、通常枠の点数を維持。\"`, `      reason:\n        candidatePromotionTickets.length\n          ? \"通常枠と検証済み独立展開を維持し、3着まで物理根拠がそろうpriority 90以上の候補だけを空き枠へ補完。\"\n          : addedTickets.length\n            ? \"通常枠とは別に、時系列と艇・着順・役割が一致した独立展開だけを追加。\"\n            : \"購入可能な独立展開がないため、通常枠の点数を維持。\"`);
replaceOnce(`        ticketPolicyVersion:\n          \"practical-5-7-10-grounded-flow2-v2\"`, `        ticketPolicyVersion:\n          \"practical-5-7-10-grounded-flow2-candidate90-v3\"`);
replaceOnce(`      reason:\n        selected.length >\n          NORMAL_MAXIMUM_COUNT\n          ? \"基本5〜7点に、検証済みの独立展開だけを追加。\"\n          : \"展開とコースから基本5〜7点を構成。\"`, `      reason:\n        candidatePromotionTickets.length\n          ? \"基本5〜7点と検証済み独立展開を維持し、priority 90以上かつ3着まで物理根拠がそろう候補だけを空き枠へ補完。\"\n          : selected.length >\n              NORMAL_MAXIMUM_COUNT\n            ? \"基本5〜7点に、検証済みの独立展開だけを追加。\"\n            : \"展開とコースから基本5〜7点を構成。\"`);
replaceOnce(`    MINIMUM_FLOW_ROLE_SCORE,\n`, `    MINIMUM_FLOW_ROLE_SCORE,\n    MINIMUM_CANDIDATE_PROMOTION_SCORE,\n`);
fs.writeFileSync(file,s);
console.log("candidate90 production patch applied");
