"use strict";
const fs=require("node:fs"),path=require("node:path");global.window=global;require("../js/ai-core");require("../js/third-six-rescue-fixed5");require("../js/escape-outer-second-rescue-fixed5");const core=global.ChappyAICore,DIR=path.join(process.cwd(),"data","predictions"),H="20260812",KEYS=["st","ex","flow","attack","hold","pickup"];
function rows(d){return[...(d.predictions||[]),...(d.verificationPredictions||[])];}function ticket(v){const p=String(v?.ticket||v||"").match(/[1-6]/g)||[];return p.length>=3?p.slice(0,3).join("-"):"";}function input(r){const s=r?.prediction?.preRaceConditions||r?.preRaceConditions;if(!s||!Array.isArray(s.boats)||s.boats.length<6)return null;return{...s,entries:s.boats,boats:s.boats,jcd:r.jcd,stadiumCode:r.jcd,venueCode:r.jcd,place:r.place,placeName:r.place,venueName:r.place,raceNo:r.raceNo,rno:r.raceNo,weather:s.weather||{}};}function fixed(p){const f=p.formations||{};return[...(f.main||[]).slice(0,3),...(f.safety||[]).slice(0,2)].map(ticket).filter(Boolean);}function boatNo(x,i){return Number(x?.boatNo??x?.boat??x?.no??i+1);}function find(a,n){return(a||[]).find((x,i)=>boatNo(x,i)===n)||null;}function num(v){const n=Number(v);return Number.isFinite(n)?n:0;}function metric(b,k){const m={st:["indexes.st","st"],ex:["indexes.exhibition","indexes.ex","exhibition","ex"],flow:["indexes.raceFlow","raceFlow"],attack:["roleScores.attack","attack"],hold:["roleScores.hold","hold"],pickup:["roleScores.pickup","pickup"]}[k];for(const p of m){let v=b;for(const q of p.split("."))v=v?.[q];if(Number.isFinite(Number(v)))return Number(v);}return 0;}function analyses(p){return p?.analyses||p?.evaluations||p?.boatEvaluation?.evaluations||[];}function init(){return{n:0,sum:Object.fromEntries(KEYS.map(k=>[k,0])),actualThird:{}};}function add(g,diffs,a3){g.n++;g.actualThird[a3]=(g.actualThird[a3]||0)+1;for(const k of KEYS)g.sum[k]+=diffs[k];}function finish(g){return{n:g.n,actualThird:g.actualThird,mean:Object.fromEntries(KEYS.map(k=>[k,g.n?Math.round(g.sum[k]/g.n*100)/100:0]))};}
const P={discovery:{hit:init(),miss:init()},holdout:{hit:init(),miss:init()}},seen=new Set();for(const fn of fs.readdirSync(DIR).filter(x=>/^\d{8}\.json$/.test(x)).sort()){const date=fn.slice(0,8),d=JSON.parse(fs.readFileSync(path.join(DIR,fn),"utf8"));for(const r of rows(d)){if(r?.result?.settled!==true)continue;const rk=r.raceKey||`${date}-${r.jcd}-${r.raceNo}`;if(seen.has(rk))continue;seen.add(rk);try{const i=input(r),actual=ticket(r?.result?.resultTicket||r?.result?.review?.resultTicket);if(!i||!actual)continue;const [a1,a2,a3]=actual.split("-").map(Number);if(a1!==1||a2!==3)continue;const p=core.buildPredictionData(i),base=fixed(p);if(!base.includes("1-3-4"))continue;const a=analyses(p),b4=find(a,4);if(!b4)continue;const q=P[date<H?"discovery":"holdout"];if(a3===4&&base.includes(actual)){const diffs={};for(const k of KEYS){const alt=Math.max(...[2,5,6].map(n=>metric(find(a,n),k)));diffs[k]=alt-metric(b4,k);}add(q.hit,diffs,a3);}else if([2,5,6].includes(a3)&&!base.includes(actual)){const ba=find(a,a3);if(!ba)continue;const diffs={};for(const k of KEYS)diffs[k]=metric(ba,k)-metric(b4,k);add(q.miss,diffs,a3);}}catch{}}}
const out={schemaVersion:1,holdoutStart:H,definition:"1-3 pair with current third=4: compare correct 1-3-4 hits vs misses where actual third is 2/5/6",periods:{}};for(const[k,v]of Object.entries(P)){const hit=finish(v.hit),miss=finish(v.miss);out.periods[k]={hit,miss,missMinusHit:Object.fromEntries(KEYS.map(key=>[key,Math.round((miss.mean[key]-hit.mean[key])*100)/100]))};}out.notes={productionChanged:false,oddsUsed:false,interpretation:"positive missMinusHit means alternative third boats are relatively stronger vs boat4 in miss group"};console.log(JSON.stringify(out,null,2));