"use strict";
const fs=require("node:fs"),path=require("node:path");
const root=path.resolve(__dirname,".."),stats=path.join(root,"data","stats");
const GATE=path.join(stats,"unified-improvement-decision-gate.json"),OUT=path.join(stats,"phase3-learning-handoff.json");
function build(gate){if(!gate||gate.allSourcesConnected!==true)throw new Error("unified gate is not fully connected");const items=Array.isArray(gate.items)?gate.items:[];const candidates=items.filter(x=>x.status==="available"&&x.decision==="candidate").map(x=>({id:x.id,sourceFile:x.file,affectedSettledCount:x.affectedSettledCount,minimumAffectedSettledCount:x.minimumAffectedSettledCount,aRecoveryRate:x.aRecoveryRate,bRecoveryRate:x.bRecoveryRate,aProfit:x.aProfit,bProfit:x.bProfit,status:"awaiting-user-approval",approved:false,productionApplied:false}));const blocked=items.filter(x=>x.decision==="blocked").map(x=>({id:x.id,reason:x.reason}));return{schemaVersion:1,generatedAt:new Date().toISOString(),phase:"phase3",implementationComplete:true,productionChanged:false,automaticApplication:false,requiresUserApproval:true,allSourcesConnected:true,policy:"A/Bでcandidateになった改善だけ承認候補へ渡す。承認前は本番予想へ一切反映しない。reject/continue/review/blockedは本番適用不可。",candidateCount:candidates.length,candidates,blockedCount:blocked.length,blocked,nextStep:candidates.length?"user-approval":"collect-more-settled-races"};}
function main(){const gate=JSON.parse(fs.readFileSync(GATE,"utf8")),r=build(gate);fs.writeFileSync(OUT,JSON.stringify(r,null,2)+"\n");console.log(JSON.stringify(r,null,2));}
if(require.main===module)main();module.exports={build};
