"use strict";
const fs=require('node:fs'),path=require('node:path'),assert=require('node:assert');global.window=global;require('../js/boat-identity');require('../js/ai-core');require('../js/prediction');const sel=require('../js/practical-selection'),dir=path.join(process.cwd(),'data','predictions');
const rows=d=>[...(d.predictions||[]),...(d.verificationPredictions||[])];const tk=v=>{const m=String(v?.ticket||v||'').match(/[1-6]/g)||[];return m.length>=3?m.slice(0,3).join('-'):''};const dataOf=r=>{const s=r?.prediction?.preRaceConditions||r?.preRaceConditions;if(!s||!Array.isArray(s.boats)||s.boats.length<5)return null;return{...s,entries:s.boats,boats:s.boats,jcd:r.jcd,stadiumCode:r.jcd,venueCode:r.jcd,placeName:r.place,venueName:r.place,raceNo:r.raceNo,rno:r.raceNo,weather:s.weather||{}}};
const got={pre:{r:0,h:0,stake:0},mid:{r:0,h:0,stake:0},recent:{r:0,h:0,stake:0}},seen=new Set();for(const f of fs.readdirSync(dir).filter(x=>/^\d{8}\.json$/.test(x)).sort()){const date=f.slice(0,8);if(date>'20260811')continue;const n=+date,d=JSON.parse(fs.readFileSync(path.join(dir,f),'utf8'));for(const r of rows(d)){if(r?.result?.settled!==true)continue;const key=r.raceKey||`${date}-${r.jcd}-${r.raceNo}`;if(seen.has(key))continue;seen.add(key);const data=dataOf(r),actual=tk(r?.result?.resultTicket||r?.result?.review?.resultTicket);if(!data||!actual)continue;const s=sel.select(global.createPrediction(data)),ts=(s?.tickets||[]).map(x=>tk(x?.ticket||x));const k=n<20260807?'pre':n<=20260810?'mid':'recent';got[k].r++;got[k].stake+=ts.length*100;if(ts.includes(actual))got[k].h++;const bad=(s?.tickets||[]).filter(x=>x?.selectionTier==='候補補完'&&Number(tk(x?.ticket||x)[0])>=4);assert.equal(bad.length,0,`${key}: outer candidate promotion remains selected`);}}
assert.deepStrictEqual(got,{pre:{r:457,h:134,stake:380200},mid:{r:313,h:107,stake:264200},recent:{r:97,h:28,stake:86300}});console.log('outer candidate promotion prune regression: OK',got);
