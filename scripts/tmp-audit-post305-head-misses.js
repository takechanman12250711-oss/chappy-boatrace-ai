"use strict";
const fs=require("node:fs"),path=require("node:path");global.window=global;require("../js/ai-core");const core=global.ChappyAICore,dir=path.join(process.cwd(),"data","predictions"),rows=d=>[...(d.predictions||[]),...(d.verificationPredictions||[])];
function dataOf(r){const s=r?.prediction?.preRaceConditions||r?.preRaceConditions;if(!s||!Array.isArray(s.boats)||s.boats.length<5)return null;return{...s,entries:s.boats,boats:s.boats,jcd:r.jcd,stadiumCode:r.jcd,venueCode:r.jcd,placeName:r.place,venueName:r.place,raceNo:r.raceNo,rno:r.raceNo,weather:s.weather||{}};}function ticket(v){const m=String(v?.ticket||v||"").match(/[1-6]/g)||[];return m.length>=3?m.slice(0,3).map(Number):null;}function actualHead(r){const t=ticket(r?.result?.resultTicket||r?.result?.review?.resultTicket);return t?t[0]:0;}function scenarioHead(s){return Number(s?.attacker||0);}function avg(a){return a.length?a.reduce((x,y)=>x+y,0)/a.length:0;}function q(a,p){if(!a.length)return 0;const b=[...a].sort((x,y)=>x-y),i=Math.min(b.length-1,Math.max(0,Math.floor((b.length-1)*p)));return b[i];}
const out={settled:0,replayed:0,headCorrect:0,headMiss:0,predictedHead:{},actualHead:{},missByActualHead:{},missActualScenarioRank:{},missGapToMain:{},missByMainType:{},sashiTiebreakApplied:0,sashiTiebreakCorrect:0,sashiTiebreakWrong:0,outsideHeadMisses:{5:0,6:0},examples:[]};const gaps={2:[],3:[],4:[]},ranks={2:{},3:{},4:{}};
for(const f of fs.readdirSync(dir).filter(x=>/^202608(0[7-9]|10)\.json$/.test(x)).sort()){const d=JSON.parse(fs.readFileSync(path.join(dir,f),"utf8"));for(const r of rows(d)){if(r?.result?.settled!==true)continue;out.settled++;const data=dataOf(r),ah=actualHead(r);if(!data||!ah)continue;const ai=core.buildPredictionData(data),rs=ai?.raceScenarios||{},mh=scenarioHead(rs.mainScenario),mt=String(rs.mainScenario?.type||"unknown");out.replayed++;out.predictedHead[mh]=(out.predictedHead[mh]||0)+1;out.actualHead[ah]=(out.actualHead[ah]||0)+1;const tb=rs?.evidence?.sashiSkillTiebreak;if(tb?.applied){out.sashiTiebreakApplied++;if(mh===ah)out.sashiTiebreakCorrect++;else out.sashiTiebreakWrong++;}if(mh===ah){out.headCorrect++;continue;}out.headMiss++;out.missByActualHead[ah]=(out.missByActualHead[ah]||0)+1;out.missByMainType[mt]=(out.missByMainType[mt]||0)+1;if(ah===5||ah===6){out.outsideHeadMisses[ah]++;if(out.examples.length<20)out.examples.push({raceKey:r.raceKey,actualHead:ah,main:mt,mainScore:rs.mainScenario?.score});continue;}const targetType=ah===1?"escape":ah===2?"sashi":ah===3?"threeAttack":"fourAttack",target=(rs.scenarios||[]).find(s=>s.type===targetType);if(target){const rank=Number(target.rank||((rs.scenarios||[]).indexOf(target)+1));const gap=Number(rs.mainScenario?.score||0)-Number(target.score||0);out.missActualScenarioRank[`${ah}:${rank}`]=(out.missActualScenarioRank[`${ah}:${rank}`]||0)+1;out.missGapToMain[ah]=out.missGapToMain[ah]||{n:0,avg:0,median:0,p75:0,le1:0,le2_5:0,le5:0};if(gaps[ah])gaps[ah].push(gap);if(ranks[ah])ranks[ah][rank]=(ranks[ah][rank]||0)+1;if(out.examples.length<20)out.examples.push({raceKey:r.raceKey,actualHead:ah,main:mt,target:targetType,rank,gap:Number(gap.toFixed(2)),targetScore:target.score,mainScore:rs.mainScenario?.score,tb:tb?.applied||false});}}
}
for(const h of[2,3,4]){const a=gaps[h];out.missGapToMain[h]={n:a.length,avg:Number(avg(a).toFixed(2)),median:Number(q(a,.5).toFixed(2)),p75:Number(q(a,.75).toFixed(2)),le1:a.filter(x=>x<=1).length,le2_5:a.filter(x=>x<=2.5).length,le5:a.filter(x=>x<=5).length,ranks:ranks[h]};}
console.log(JSON.stringify(out,null,2));