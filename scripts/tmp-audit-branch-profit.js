"use strict";
const fs=require('node:fs'),path=require('node:path');global.window=global;require('../js/boat-identity');require('../js/ai-core');require('../js/prediction');const sel=require('../js/practical-selection'),dir=path.join(process.cwd(),'data','predictions');
const rows=d=>[...(d.predictions||[]),...(d.verificationPredictions||[])];const tk=v=>{const m=String(v?.ticket||v||'').match(/[1-6]/g)||[];return m.length>=3?m.slice(0,3).join('-'):''};const dataOf=r=>{const s=r?.prediction?.preRaceConditions||r?.preRaceConditions;if(!s||!Array.isArray(s.boats)||s.boats.length<5)return null;return{...s,entries:s.boats,boats:s.boats,jcd:r.jcd,stadiumCode:r.jcd,venueCode:r.jcd,placeName:r.place,venueName:r.place,raceNo:r.raceNo,rno:r.raceNo,weather:s.weather||{}}};const pay=r=>Number(r?.result?.payout||r?.result?.officialPayoutPer100||r?.result?.review?.payout||0);
function bucket(){return{selected:0,hits:0,stake:0,ret:0}}function add(b,hit,p){b.selected++;b.stake+=100;if(hit){b.hits++;b.ret+=p}}function done(o){for(const b of Object.values(o)){b.roi=b.stake?+(b.ret/b.stake*100).toFixed(2):0;b.profit=b.ret-b.stake;}return o}
function branchOf(row,i){const s=String(row?.category||row?.sourceCategory||row?.source||row?.reason||'').toLowerCase();const t=tk(row?.ticket||row);const h=Number(t[0]||0);if(s.includes('候補補完')||s.includes('candidate'))return 'candidate-promotion';if(s.includes('2差')||s.includes('sashi')||h===2)return '2-sashi';if(s.includes('3攻')||s.includes('three')||h===3)return '3-attack';if(s.includes('4カド')||s.includes('corner')||h===4)return '4-corner';if(i>=7)return 'other-late';return 'core';}
const periods={pre:{},mid:{},recent:{}},seen=new Set();for(const f of fs.readdirSync(dir).filter(x=>/^\d{8}\.json$/.test(x)).sort()){const date=f.slice(0,8),n=+date,d=JSON.parse(fs.readFileSync(path.join(dir,f),'utf8'));for(const r of rows(d)){if(r?.result?.settled!==true)continue;const key=r.raceKey||`${date}-${r.jcd}-${r.raceNo}`;if(seen.has(key))continue;seen.add(key);const data=dataOf(r),actual=tk(r?.result?.resultTicket||r?.result?.review?.resultTicket);if(!data||!actual)continue;const p=global.createPrediction(data),s=sel.select(p),trs=s?.ticketRows||s?.rows||s?.selectedRows||s?.tickets||[];const k=n<20260807?'pre':n<=20260810?'mid':'recent';trs.forEach((row,i)=>{if(i<7)return;const t=tk(row?.ticket||row);if(!t)return;const bkey=branchOf(row,i);periods[k][bkey]??=bucket();add(periods[k][bkey],t===actual,pay(r));});}}
const out={pre:done(periods.pre),mid:done(periods.mid),recent:done(periods.recent)};fs.mkdirSync('tmp-analysis-output',{recursive:true});fs.writeFileSync('tmp-analysis-output/post322-branch-profit.json',JSON.stringify(out,null,2));console.log(JSON.stringify(out,null,2));