"use strict";
const fs=require("node:fs"),path=require("node:path");global.window=global;require("../js/ai-core");const core=global.ChappyAICore,dir=path.join(process.cwd(),"data","predictions"),rows=d=>[...(d.predictions||[]),...(d.verificationPredictions||[])];
function dataOf(r){const s=r?.prediction?.preRaceConditions||r?.preRaceConditions;if(!s||!Array.isArray(s.boats)||s.boats.length<5)return null;return{...s,entries:s.boats,boats:s.boats,jcd:r.jcd,stadiumCode:r.jcd,venueCode:r.jcd,placeName:r.place,venueName:r.place,raceNo:r.raceNo,rno:r.raceNo,weather:s.weather||{}};}function parts(v){const m=String(v?.ticket||v||"").match(/[1-6]/g)||[];return m.length>=3?m.slice(0,3).map(Number):null;}function key(v){const x=parts(v);return x?x.join("-"):"";}function list(v){return(Array.isArray(v)?v:[]).map(key).filter(Boolean);}function uniq(a){return[...new Set(a)];}
function generateForHead(head, seconds, thirds, limit, out){for(const s of seconds){if(s===head)continue;for(const t of thirds){if(t===head||t===s)continue;const z=`${head}-${s}-${t}`;if(!out.includes(z))out.push(z);if(out.length>=limit)return;}}}
function altSafety(data,ai){const rs=ai.raceScenarios||{},cur=list(ai.formations?.safety),heads=uniq(cur.map(x=>Number(x[0]))),out=[];for(const h of heads){const sc=(rs.scenarios||[]).find(s=>Number(s.attacker)===h);let seconds=[],thirds=[];if(sc){const hp=core.buildHoldPickupTheory(data.entries||data.boats,ai.analyses,sc,rs.wallTheory||{}, {attackerBoatNo:h,attackerCourse:Number(sc.attacker||h),blockedBoats:Array.isArray(sc.blockedBoats)?sc.blockedBoats:[]});seconds=(hp.secondCandidates||[]).map(x=>Number(x.boatNo));thirds=(hp.thirdCandidates||[]).map(x=>Number(x.boatNo));}else{const hp=rs.holdPickupTheory||{};seconds=(hp.secondCandidates||[]).map(x=>Number(x.boatNo));thirds=(hp.thirdCandidates||[]).map(x=>Number(x.boatNo));}generateForHead(h,seconds,thirds,8,out);if(out.length>=8)break;}return out;}
const rec=[];for(const f of fs.readdirSync(dir).filter(x=>/^202608(0[7-9]|10)\.json$/.test(x)).sort()){const d=JSON.parse(fs.readFileSync(path.join(dir,f),"utf8"));for(const r of rows(d)){if(r?.result?.settled!==true)continue;const data=dataOf(r),a=key(r?.result?.resultTicket||r?.result?.review?.resultTicket);if(!data||!a)continue;const ai=core.buildPredictionData(data),cur=list(ai.formations?.safety),alt=altSafety(data,ai),main=list(ai.formations?.main),mh=Number(ai?.raceScenarios?.mainScenario?.attacker||0);rec.push({date:Number(f.slice(0,8)),actual:a,cur,alt,main,headMiss:mh!==Number(a[0])});}}
function met(xs,which){let s8=0,s2=0,m5=0,g8=0,l8=0,g2=0,l2=0,hm8=0,hm2=0;for(const x of xs){const a=x.actual,c=x.cur,n=x.alt,use=which==='cur'?c:n;const c8=c.includes(a),n8=n.includes(a),c2=c.slice(0,2).includes(a),n2=n.slice(0,2).includes(a);if(use.includes(a))s8++;if(use.slice(0,2).includes(a))s2++;if([...x.main.slice(0,3),...use.slice(0,2)].includes(a))m5++;if(!c8&&n8)g8++;if(c8&&!n8)l8++;if(!c2&&n2)g2++;if(c2&&!n2)l2++;if(x.headMiss&&use.includes(a))hm8++;if(x.headMiss&&use.slice(0,2).includes(a))hm2++;}return{n:xs.length,safety8:s8,safetyFirst2:s2,main3cover2:m5,gain8:g8,loss8:l8,gain2:g2,loss2:l2,headMissSafety8:hm8,headMissFirst2:hm2};}
const train=rec.filter(x=>x.date<=20260808),test=rec.filter(x=>x.date>=20260809);console.log(JSON.stringify({all:{current:met(rec,'cur'),alternative:met(rec,'alt')},train:{current:met(train,'cur'),alternative:met(train,'alt')},test:{current:met(test,'cur'),alternative:met(test,'alt')}},null,2));