"use strict";
const fs=require("node:fs"),path=require("node:path");global.window=global;require("../js/ai-core");const core=global.ChappyAICore,DIR=path.join(process.cwd(),"data","predictions"),H="20260812";
function rows(d){return[...(d.predictions||[]),...(d.verificationPredictions||[])];}function ticket(v){const p=String(v?.ticket||v||"").match(/[1-6]/g)||[];return p.length>=3?p.slice(0,3).join("-"):"";}function input(r){const s=r?.prediction?.preRaceConditions||r?.preRaceConditions;if(!s||!Array.isArray(s.boats)||s.boats.length<6)return null;return{...s,entries:s.boats,boats:s.boats,jcd:r.jcd,stadiumCode:r.jcd,venueCode:r.jcd,place:r.place,placeName:r.place,venueName:r.place,raceNo:r.raceNo,rno:r.raceNo,weather:s.weather||{}};}function fixed(p){const f=p.formations||{};return[...(f.main||[]).slice(0,3),...(f.safety||[]).slice(0,2)].map(ticket).filter(Boolean);}function add(o,k){o[k]=(o[k]||0)+1;}function top(o,n=15){return Object.entries(o).sort((a,b)=>b[1]-a[1]).slice(0,n).map(([key,races])=>({key,races}));}
const P={discovery:{total:0,pairs:{},actualSecond:{},mainType:{},pairMainType:{}},holdout:{total:0,pairs:{},actualSecond:{},mainType:{},pairMainType:{}}},seen=new Set();for(const fn of fs.readdirSync(DIR).filter(x=>/^\d{8}\.json$/.test(x)).sort()){const date=fn.slice(0,8),d=JSON.parse(fs.readFileSync(path.join(DIR,fn),"utf8"));for(const r of rows(d)){if(r?.result?.settled!==true)continue;const key=r.raceKey||`${date}-${r.jcd}-${r.raceNo}`;if(seen.has(key))continue;seen.add(key);try{const i=input(r),actual=ticket(r?.result?.resultTicket||r?.result?.review?.resultTicket);if(!i||!actual)continue;const p=core.buildPredictionData(i),base=fixed(p);if(base.includes(actual))continue;const [a1,a2]=actual.split("-").map(Number);const heads=base.filter(t=>Number(t[0])===a1);if(!heads.length)continue;if(heads.some(t=>Number(t.split("-")[1])===a2))continue;const q=P[date<H?"discovery":"holdout"],main=p.raceScenarios?.mainScenario?.type||"unknown",predSeconds=[...new Set(heads.map(t=>Number(t.split("-")[1])))].sort().join("");q.total++;add(q.actualSecond,String(a2));add(q.mainType,main);add(q.pairs,`${a1}-${a2}|pred2:${predSeconds||"none"}`);add(q.pairMainType,`${a1}-${a2}|${main}`);}catch{}}}
const out={schemaVersion:1,definition:"fixed5 miss where actual winner exists as head in fixed5 but actual second does not appear behind that head",holdoutStart:H,periods:{}};for(const[k,v]of Object.entries(P))out.periods[k]={total:v.total,actualSecond:top(v.actualSecond,6),mainType:top(v.mainType,10),topPairStructures:top(v.pairs,20),topPairMainTypes:top(v.pairMainType,20)};out.notes={productionChanged:false,oddsUsed:false,goal:"find largest reproducible second-place miss structure before any local A/B"};console.log(JSON.stringify(out,null,2));