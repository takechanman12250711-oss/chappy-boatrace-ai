"use strict";
const fs=require('node:fs'),path=require('node:path');global.window=global;require('../js/boat-identity');require('../js/ai-core');require('../js/prediction');const core=global.ChappyAICore,sel=require('../js/practical-selection'),dir=path.join(process.cwd(),'data','predictions');
const rows=d=>[...(d.predictions||[]),...(d.verificationPredictions||[])];const tk=v=>{const m=String(v?.ticket||v||'').match(/[1-6]/g)||[];return m.length>=3?m.slice(0,3).join('-'):''};const dataOf=r=>{const s=r?.prediction?.preRaceConditions||r?.preRaceConditions;if(!s||!Array.isArray(s.boats)||s.boats.length<5)return null;return{...s,entries:s.boats,boats:s.boats,jcd:r.jcd,stadiumCode:r.jcd,venueCode:r.jcd,placeName:r.place,venueName:r.place,raceNo:r.raceNo,rno:r.raceNo,weather:s.weather||{}}};const pay=r=>Number(r?.result?.payout||r?.result?.officialPayoutPer100||r?.result?.review?.payout||0);function findRS(o,s=new Set()){if(!o||typeof o!=='object'||s.has(o))return null;s.add(o);if(o.mainScenario&&Array.isArray(o.scenarios))return o;for(const v of Object.values(o)){const r=findRS(v,s);if(r)return r;}return null;}function B(){return{r:0,baseH:0,newH:0,baseStake:0,newStake:0,baseRet:0,newRet:0,trimmed:0,removed:0,lostHits:0};}function F(s){return{...s,baseRate:s.r?+(s.baseH/s.r*100).toFixed(2):0,newRate:s.r?+(s.newH/s.r*100).toFixed(2):0,baseRoi:s.baseStake?+(s.baseRet/s.baseStake*100).toFixed(2):0,newRoi:s.newStake?+(s.newRet/s.newStake*100).toFixed(2):0,profitDelta:(s.newRet-s.newStake)-(s.baseRet-s.baseStake)};}
const all=[];const seen=new Set();for(const f of fs.readdirSync(dir).filter(x=>/^\d{8}\.json$/.test(x)).sort()){const date=f.slice(0,8),n=Number(date),d=JSON.parse(fs.readFileSync(path.join(dir,f),'utf8'));for(const r of rows(d)){if(r?.result?.settled!==true)continue;const key=r.raceKey||`${date}-${r.jcd}-${r.raceNo}`;if(seen.has(key))continue;seen.add(key);const actual=tk(r?.result?.resultTicket||r?.result?.review?.resultTicket),data=dataOf(r);if(!data||!actual)continue;const p=global.createPrediction(data),s=sel.select(p),base=(s?.tickets||[]).map(x=>tk(x?.ticket||x)).filter(Boolean),ai=core.buildPredictionData(data),rs=findRS(ai);if(!rs)continue;const main=rs.mainScenario||{},other=(rs.scenarios||[]).filter(x=>x!==main).map(x=>Number(x?.score||0)).sort((a,b)=>b-a)[0]??0,gap=Number(main.score||0)-other,eligible=main.type==='escape'&&Number(main.score||0)>=80&&gap>=5;let next=base;if(eligible){const one=base.filter(x=>Number(x[0])===1),alt=base.filter(x=>Number(x[0])!==1).slice(0,1);next=[...one,...alt];}all.push({date,n,actual,payout:pay(r),base,next,eligible});}}
const pre=all.filter(x=>x.n<20260807),target=all.filter(x=>x.n>=20260807&&x.n<=20260810);const preDates=[...new Set(pre.map(x=>x.date))].sort(),cut=Math.ceil(preDates.length/2),groups={preEarly:pre.filter(x=>preDates.slice(0,cut).includes(x.date)),preLate:pre.filter(x=>preDates.slice(cut).includes(x.date)),d0807_08:target.filter(x=>x.n<=20260808),d0809_10:target.filter(x=>x.n>=20260809)};
const out={};for(const [name,arr] of Object.entries(groups)){const s=B();for(const x of arr){s.r++;const bh=x.base.includes(x.actual),nh=x.next.includes(x.actual);if(bh)s.baseH++;if(nh)s.newH++;s.baseStake+=x.base.length*100;s.newStake+=x.next.length*100;if(bh)s.baseRet+=x.payout;if(nh)s.newRet+=x.payout;if(x.next.length<x.base.length){s.trimmed++;s.removed+=x.base.length-x.next.length;}if(bh&&!nh)s.lostHits++;}out[name]=F(s);}fs.mkdirSync('tmp-analysis-output',{recursive:true});fs.writeFileSync('tmp-analysis-output/one-head-trim-fixed.json',JSON.stringify(out,null,2));console.log(JSON.stringify(out,null,2));