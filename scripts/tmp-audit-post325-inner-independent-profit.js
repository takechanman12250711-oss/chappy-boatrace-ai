"use strict";
const fs=require('node:fs'),path=require('node:path');global.window=global;require('../js/boat-identity');require('../js/ai-core');require('../js/prediction');const sel=require('../js/practical-selection'),dir=path.join(process.cwd(),'data','predictions');
const rows=d=>[...(d.predictions||[]),...(d.verificationPredictions||[])];const tk=v=>{const m=String(v?.ticket||v||'').match(/[1-6]/g)||[];return m.length>=3?m.slice(0,3).join('-'):''};const dataOf=r=>{const s=r?.prediction?.preRaceConditions||r?.preRaceConditions;if(!s||!Array.isArray(s.boats)||s.boats.length<5)return null;return{...s,entries:s.boats,boats:s.boats,jcd:r.jcd,stadiumCode:r.jcd,venueCode:r.jcd,placeName:r.place,venueName:r.place,raceNo:r.raceNo,rno:r.raceNo,weather:s.weather||{}}};const pay=r=>Number(r?.result?.payout||r?.result?.officialPayoutPer100||r?.result?.review?.payout||0);
function b(){return{selected:0,hits:0,stake:0,ret:0}} function add(x,hit,p){x.selected++;x.stake+=100;if(hit){x.hits++;x.ret+=p}} function done(o){for(const x of Object.values(o)){x.roi=x.stake?+(x.ret/x.stake*100).toFixed(2):0;x.profit=x.ret-x.stake;}return o}
function band(p){p=Number(p||0);if(p>=90)return'90+';if(p>=85)return'85-89';if(p>=80)return'80-84';if(p>=75)return'75-79';if(p>=70)return'70-74';return'65-69';}
const periods={pre:{},mid:{},recent:{}},seen=new Set();for(const f of fs.readdirSync(dir).filter(x=>/^\d{8}\.json$/.test(x)).sort()){const date=f.slice(0,8),n=+date,d=JSON.parse(fs.readFileSync(path.join(dir,f),'utf8'));for(const r of rows(d)){if(r?.result?.settled!==true)continue;const key=r.raceKey||`${date}-${r.jcd}-${r.raceNo}`;if(seen.has(key))continue;seen.add(key);const data=dataOf(r),actual=tk(r?.result?.resultTicket||r?.result?.review?.resultTicket);if(!data||!actual)continue;const p=global.createPrediction(data),s=sel.select(p),k=n<20260807?'pre':n<=20260810?'mid':'recent';const branchMap=new Map((p?.aiCore?.formations?.evidence?.branches||[]).map(x=>[String(x?.id||''),x]));for(const row of s?.tickets||[]){if(String(row?.selectionTier||'')!=='展開追加')continue;const t=tk(row?.ticket||row),h=Number(t[0]||0),bd=band(row?.priorityScore);if(h<1||h>4)continue;let key2=`head${h}|${bd}`;if(h===1&&bd==='75-79'){const bs=(row?.validIndependentBranchIds||[]).map(id=>branchMap.get(String(id))).filter(Boolean);const kinds=[...new Set(bs.map(x=>String(x?.phaseEvidence?.kind||'unknown')))];const exact=bs.some(x=>x?.exactGoalOrder===true)?'exact':'nonexact';key2+=`|${kinds.sort().join('+')||'none'}|${exact}`;}periods[k][key2]??=b();add(periods[k][key2],t===actual,pay(r));}}}
const out={pre:done(periods.pre),mid:done(periods.mid),recent:done(periods.recent)};fs.mkdirSync('tmp-analysis-output',{recursive:true});fs.writeFileSync('tmp-analysis-output/post325-inner-independent-profit.json',JSON.stringify(out,null,2));console.log(JSON.stringify(out,null,2));