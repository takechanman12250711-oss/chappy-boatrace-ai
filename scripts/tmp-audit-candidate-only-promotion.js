"use strict";
const fs=require("node:fs"),path=require("node:path");global.window=global;require("../js/boat-identity");require("../js/ai-core");require("../js/prediction");const selector=require("../js/practical-selection"),dir=path.join(process.cwd(),"data","predictions"),rows=d=>[...(d.predictions||[]),...(d.verificationPredictions||[])];
function tk(v){const m=String(v?.ticket||v||"").match(/[1-6]/g)||[];return m.length>=3?m.slice(0,3).join("-"):"";}function dataOf(r){const s=r?.prediction?.preRaceConditions||r?.preRaceConditions;if(!s||!Array.isArray(s.boats)||s.boats.length<5)return null;return{...s,entries:s.boats,boats:s.boats,jcd:r.jcd,stadiumCode:r.jcd,venueCode:r.jcd,placeName:r.place,venueName:r.place,raceNo:r.raceNo,rno:r.raceNo,weather:s.weather||{}};}function structured3(d){const cov=Array.isArray(d?.physicalCoverage)?d.physicalCoverage:[];const pos=new Set(cov.map(x=>Number(x?.position||0)).filter(x=>x>=1&&x<=3));return pos.size===3;}function sourceKind(d){const ids=Array.isArray(d?.branchIds)?d.branchIds:[];if(ids.some(x=>String(x).includes("formation:flow:")))return"flow";if(ids.some(x=>String(x).includes("formation:hole:")))return"hole";if(ids.some(x=>String(x).includes("formation:main:")))return"main";if(ids.some(x=>String(x).includes("formation:cover:")))return"cover";return"other";}
const thresholds=[65,70,75,80,85,90,95];const result=Object.fromEntries(thresholds.map(t=>[t,{all:{n:0,baseHits:0,newHits:0,addedTickets:0,racesExpanded:0,gains:0},train:{n:0,baseHits:0,newHits:0,addedTickets:0,racesExpanded:0,gains:0},test:{n:0,baseHits:0,newHits:0,addedTickets:0,racesExpanded:0,gains:0}}]));const pool={total:0,structured3:0,byKind:{},actualMatches:0,priorityActual:[],priorityAll:[]};
for(const f of fs.readdirSync(dir).filter(x=>/^202608(0[7-9]|10)\.json$/.test(x)).sort()){const date=Number(f.slice(0,8)),d=JSON.parse(fs.readFileSync(path.join(dir,f),"utf8"));for(const r of rows(d)){if(r?.result?.settled!==true)continue;const data=dataOf(r),actual=tk(r?.result?.resultTicket||r?.result?.review?.resultTicket);if(!data||!actual)continue;const p=global.createPrediction(data),sel=selector.select(p),base=(sel?.tickets||[]).map(x=>tk(x?.ticket||x)).filter(Boolean),baseHit=base.includes(actual),raw=[...(sel?.candidateDecisions||[]),...(sel?.excludedCandidates||[])];const seen=new Set();const cand=raw.filter(x=>String(x?.reasonCode||"")==="CANDIDATE_ONLY_EVALUATION").filter(x=>{const k=tk(x?.ticket||x);if(!k||base.includes(k)||seen.has(k))return false;seen.add(k);return true;});pool.total+=cand.length;for(const c of cand){const k=sourceKind(c);pool.byKind[k]=(pool.byKind[k]||0)+1;pool.priorityAll.push(Number(c?.priorityScore||0));if(structured3(c)){pool.structured3++;if(tk(c?.ticket||c)===actual){pool.actualMatches++;pool.priorityActual.push(Number(c?.priorityScore||0));}}}
for(const t of thresholds){const parts=[result[t].all,date<=20260808?result[t].train:result[t].test];for(const x of parts){x.n++;if(baseHit)x.baseHits++;}const capacity=Math.max(0,10-base.length);const eligible=cand.filter(c=>structured3(c)&&Number(c?.priorityScore||0)>=t).sort((a,b)=>Number(b?.priorityScore||0)-Number(a?.priorityScore||0)||tk(a?.ticket||a).localeCompare(tk(b?.ticket||b))).slice(0,capacity);const promoted=eligible.map(c=>tk(c?.ticket||c));const hit=baseHit||promoted.includes(actual);for(const x of parts){if(eligible.length){x.racesExpanded++;x.addedTickets+=eligible.length;}if(hit)x.newHits++;if(!baseHit&&hit)x.gains++;}}
}}
for(const t of thresholds){for(const k of["all","train","test"]){const x=result[t][k];x.hitRate=x.n?Number((x.newHits/x.n*100).toFixed(2)):0;x.baseRate=x.n?Number((x.baseHits/x.n*100).toFixed(2)):0;x.gainPer100Added=x.addedTickets?Number((x.gains/x.addedTickets*100).toFixed(2)):0;x.avgAdded=x.n?Number((x.addedTickets/x.n).toFixed(3)):0;}}
pool.priorityAll.sort((a,b)=>a-b);pool.priorityActual.sort((a,b)=>a-b);function q(a,p){if(!a.length)return null;return a[Math.min(a.length-1,Math.floor((a.length-1)*p))];}pool.prioritySummary={all:{min:q(pool.priorityAll,0),median:q(pool.priorityAll,.5),p75:q(pool.priorityAll,.75),max:q(pool.priorityAll,1)},actual:{min:q(pool.priorityActual,0),median:q(pool.priorityActual,.5),p75:q(pool.priorityActual,.75),max:q(pool.priorityActual,1)}};console.log(JSON.stringify({pool,result},null,2));