"use strict";
const fs=require("node:fs"),path=require("node:path");
const p=path.join(process.cwd(),"js","ai-core.js");let s=fs.readFileSync(p,"utf8");
if(s.includes("function buildFormations(analyses, raceScenarios, sourceEntries = [], sourceData = {}) {")){console.log("scenario-specific safety patch already applied");process.exit(0);}
function rep(a,b,label){if(!s.includes(a))throw new Error(`patch target missing: ${label}`);s=s.replace(a,b);}
rep("function buildFormations(analyses, raceScenarios) {","function buildFormations(analyses, raceScenarios, sourceEntries = [], sourceData = {}) {","signature");
rep(`  generateTickets(\n    safety,\n    safetyHeads,\n    secondRanking.slice(0, 4),\n    thirdRanking.slice(0, 5),\n    8\n  );`,`  function scenarioSpecificSafetyRankings(head) {\n    const fallback = {\n      second: secondRanking.slice(0, 4),\n      third: thirdRanking.slice(0, 5)\n    };\n\n    if (\n      !hasScenario ||\n      !Array.isArray(sourceEntries) ||\n      sourceEntries.length < 5\n    ) {\n      return fallback;\n    }\n\n    const headNo = boatNo(head);\n    const scenario = (raceScenarios.scenarios || []).find(\n      (candidate) => Number(candidate?.attacker || 0) === headNo\n    );\n\n    if (!scenario) return fallback;\n\n    const scenarioContext = {\n      ...raceScenarios,\n      mainScenario: scenario,\n      attacker: headNo,\n      blockedBoats: [\n        ...(scenario.blockedBoats || [])\n      ]\n    };\n    const scenarioWall = buildWallTheory(\n      sourceEntries,\n      list,\n      sourceData,\n      scenarioContext\n    );\n    const scenarioHoldPickup = buildHoldPickupTheory(\n      sourceEntries,\n      list,\n      scenario,\n      scenarioWall,\n      {\n        attackerBoatNo: headNo,\n        attackerCourse: Number(scenario?.attacker || headNo),\n        blockedBoats: [\n          ...(scenario.blockedBoats || [])\n        ],\n        preservations: []\n      }\n    );\n    const byNo = new Map(\n      list.map((boat) => [boatNo(boat), boat])\n    );\n    const second = uniqueBoats(\n      (scenarioHoldPickup.secondCandidates || [])\n        .map((candidate) => byNo.get(Number(candidate?.boatNo || 0)))\n    ).slice(0, 4);\n    const third = uniqueBoats(\n      (scenarioHoldPickup.thirdCandidates || [])\n        .map((candidate) => byNo.get(Number(candidate?.boatNo || 0)))\n    ).slice(0, 5);\n\n    return {\n      second: second.length ? second : fallback.second,\n      third: third.length ? third : fallback.third\n    };\n  }\n\n  for (const head of safetyHeads) {\n    if (safety.length >= 8) break;\n    const rankings = scenarioSpecificSafetyRankings(head);\n    generateTickets(\n      safety,\n      [head],\n      rankings.second,\n      rankings.third,\n      8\n    );\n  }`,`safety generation`);
rep(`    const formations =\n      buildFormations(\n        analyses,\n        raceScenarios\n      );`,`    const formations =\n      buildFormations(\n        analyses,\n        raceScenarios,\n        entries,\n        data\n      );`,`prediction call`);
fs.writeFileSync(p,s);console.log("scenario-specific safety patch applied");