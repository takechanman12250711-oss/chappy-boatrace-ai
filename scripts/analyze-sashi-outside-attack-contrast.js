"use strict";
const fs=require("node:fs"),path=require("node:path");global.window=global;require("../js/ai-core");const core=global.ChappyAICore,DIR=path.join(process.cwd(),"data","predictions"),H="20260812";
function rows(d){return[...(d.predictions||[]),...(d.verificationPredictions||[])];}function ticket(v){const p=String(v?.ticket||v||"").match(/[1-6]/g)||[];return p.length>=3?p.slice(0,3).join("-"):"";}function input(r){const s=r?.prediction?.preRaceConditions||r?.preRaceConditions;if(!s||!Array.isArray(s.boats)||s.boats.length<6)return null;return{...s,entries:s.boats,boats:s.boats,jcd:r.jcd,stadiumCode:r.jcd,venueCode:r.jcd,place:r.place,placeName:r.place,venueName:r.place,raceNo:r.raceNo,rno:r.raceNo,weather:s.weather||{}};}function num(v){const n=Number(v);return Number.isFinite(n)?n:null;}function get(o,ps){for(const p of ps){let v=o;for(const k of p.split("."))v=v?.[k];const n=num(v);if(n!==null)return n;}return null;}function boat(a,n){return a.find((x,i)=>Number(x?.boatNo??x?.boat??x?.no??i+1)===n)||{};}function stat(a,k){const v=a.map(x=>x[k]).filter(Number.isFinite).sort((x,y)=>x-y);if(!v.length)return{n:0,avg:null,median:null};const avg=v.reduce((s,x)=>s+x,0)/v.length,mid=Math.floor(v.length/2),median=v.length%2?v[mid]:(v[mid-1]+v[mid])/2;return{n:v.length,avg:+avg.toFixed(2),median:+median.toFixed(2)};}function summarize(hit,miss){const ks=["scoreGap","st","ex","flow","attack","hold","pickup"],m={};for(const k of ks){const h=stat(hit,k),x=stat(miss,k);m[k]={hit:h,miss:x,missMinusHitAvg:h.avg===null||x.avg===null?null:+(x.avg-h.avg).toFixed(2)};}return{hit:hit.length,miss:miss.length,metrics:m};}
const groups={three:{discovery:{hit:[],miss:[]},holdout:{hit:[],miss:[]}},four:{discovery:{hit:[],miss:[]},holdout:{hit:[],miss:[]}}},seen=new Set();for(const fn of fs.readdirSync(DIR).filter(x=>/^\d{8}\.json$/.test(x)).sort()){const date=fn.slice(0,8),d=JSON.parse(fs.readFileSync(path.join(DIR,fn),"utf8"));for(const r of rows(d)){if(r?.result?.settled!==true)continue;const key=r.raceKey||`${date}-${r.jcd}-${r.raceNo}`;if(seen.has(key))continue;seen.add(key);try{const i=input(r),actual=ticket(r?.result?.resultTicket||r?.result?.review?.resultTicket);if(!i||!actual)continue;const winner=Number(actual[0]),p=core.buildPredictionData(i),rs=p.raceScenarios||{},main=rs.mainScenario;if(main?.type!=="sashi")continue;const a=p.analyses||p.evaluations||[],b2=boat(a,2),period=date<H?"discovery":"holdout";for(const [name,boatNo,type] of [["three",3,"threeAttack"],["four",4,"fourAttack"]]){const sc=(rs.scenarios||[]).find(x=>x.type===type);if(!sc)continue;const bx=boat(a,boatNo),m={scoreGap:(num(main.score)??0)-(num(sc.score)??0),st:(get(bx,["indexes.st","st"])??0)-(get(b2,["indexes.st","st"])??0),ex:(get(bx,["indexes.exhibition","indexes.ex","exhibition","ex"])??0)-(get(b2,["indexes.exhibition","indexes.ex","exhibition","ex"])??0),flow:(get(bx,["indexes.raceFlow","raceFlow"])??0)-(get(b2,["indexes.raceFlow","raceFlow"])??0),attack:(get(bx,["roleScores.attack","attack"])??0)-(get(b2,["roleScores.attack","attack"])??0),hold:(get(b2,["roleScores.hold","hold"])??0)-(get(bx,["roleScores.hold","hold"])??0),pickup:(get(bx,["roleScores.pickup","pickup"])??0)-(get(b2,["roleScores.pickup","pickup"])??0)};if(winner===2)groups[name][period].hit.push(m);else if(winner===boatNo)groups[name][period].miss.push(m);}}catch{}}}
const result={schemaVersion:1,holdoutStart:H,groups:{}};for(const[name,g]of Object.entries(groups))result.groups[name]={discovery:summarize(g.discovery.hit,g.discovery.miss),holdout:summarize(g.holdout.hit,g.holdout.miss)};result.notes={productionChanged:false,oddsUsed:false,actualResultUsedOnlyForPostraceCohortSelection:true,comparison:"main sashi correct winner2 vs same sashi main but actual winner3/4"};console.log(JSON.stringify(result,null,2));