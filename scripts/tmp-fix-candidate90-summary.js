"use strict";
const fs=require("node:fs");
const file="js/practical-selection.js";
let s=fs.readFileSync(file,"utf8");
const from=`      candidatePromotionCount:\n        candidatePromotionTickets.length,\n      candidatePromotionThreshold:\n        MINIMUM_CANDIDATE_PROMOTION_SCORE,\n      candidatePromotionTickets,\n      hasIndependentAdditions:`;
const to=`      ...(\n        candidatePromotionTickets.length\n          ? {\n              candidatePromotionCount:\n                candidatePromotionTickets.length,\n              candidatePromotionThreshold:\n                MINIMUM_CANDIDATE_PROMOTION_SCORE,\n              candidatePromotionTickets\n            }\n          : {}\n      ),\n      hasIndependentAdditions:`;
if(!s.includes(from))throw new Error("candidate promotion summary anchor not found");
s=s.replace(from,to);
fs.writeFileSync(file,s);
console.log("candidate90 summary compatibility patch applied");
