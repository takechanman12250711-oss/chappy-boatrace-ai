"use strict";
const fs=require("node:fs"),path=require("node:path");
global.window=global;require("../js/boat-identity");require("../js/ai-core");require("../js/prediction");const selector=require("../js/practical-selection"),dir=path.join(process.cwd(),"data","predictions");
const rows=d=>[...(d.predictions||[]),...(d.verificationPredictions||[])];const tk=v=>{const m=String(v?.ticket||v||"").match(/[1-6]/g)||[];return m.length>=3?m.slice(0,3).join("-"):"";};
const dataOf=r=>{const s=r?.prediction?.preRaceConditions||r?.preRaceConditions;if(!s||!Array.isArray(s.boats)||s.boats.length<5)return null;return{...s,entries:s.boats,boats:s.boats,jcd:r.jcd,stadiumCode:r.jcd,venueCode:r.jcd,placeName:r.place,venueName:r.place,raceNo:r.raceNo,rno:r.raceNo,weather:s.weather||{}};};const pay=r=>Number(r?.result?.payout||r?.result?.officialPayoutPer100||r?.result?.review?.payout||0);
function findRS(o,s=new Set()){if(!o||typeof o!=="object"||s.has(o))return null;s.add(o);if(o.mainScenario&&Array.isArray(o.scenarios))return o;for(const v of Object.values(o)){const x=findRS(v,s);if(x)return x;}return null;}
function evalRow(r,date){const data=dataOf(r),actual=tk(r?.result?.resultTicket||r?.result?.review?.resultTicket);if(!data||!actual)return null;const p=global.createPrediction(data),sel=selector.select(p),base=(sel?.tickets||[]).map(x=>tk(x?.ticket||x)).filter(Boolean),rs=findRS(p)||findRS(global.ChappyAICore?.buildPredictionData?.(data));if(!rs)return null;const main=rs.mainScenario||{},second=(rs.scenarios||[]).filter(x=>x!==main).map(x=>Number(x?.score||0)).sort((a,b)=>b-a)[0]||0;return{date,actual,payout:pay(r),base,mainType:String(main.type||""),mainScore:Number(main.score||0),gap:Number(main.score||0)-second};}
const ps={pre:[],d0807_10:[],d0811_12:[]},seen=new Set();for(const f of fs.readdirSync(dir).filter(x=>/^\d{8}\.json$/.test(x)).sort()){const date=f.slice(0,8),n=Number(date),d=JSON.parse(fs.readFileSync(path.join(dir,f),"utf8"));for(const r of rows(d)){if(r?.result?.settled!==true)continue;const key=r?.raceKey||`${date}-${r.jcd}-${r.raceNo}`;if(seen.has(key))continue;seen.add(key);const e=evalRow(r,date);if(!e)continue;if(n<20260807)ps.pre.push(e);else if(n<=20260810)ps.d0807_10.push(e);else if(n<=20260812)ps.d0811_12.push(e);}}
const scores=[80,85,90,95],gaps=[5,10,15,20];function test(arr,score,gap){let races=0,eligible=0,baseH=0,nextH=0,baseStake=0,nextStake=0,baseRet=0,nextRet=0,removed=0,lostHits=0;for(const e of arr){races++;const bh=e.base.includes(e.actual);if(bh){baseH++;baseRet+=e.payout;}baseStake+=e.base.length*100;let next=e.base;if(e.mainType==="escape"&&e.mainScore>=score&&e.gap>=gap){eligible++;next=e.base.filter(x=>x.startsWith("1-"));removed+=e.base.length-next.length;}const nh=next.includes(e.actual);if(nh){nextH++;nextRet+=e.payout;}if(bh&&!nh)lostHits++;nextStake+=next.length*100;}return{races,eligible,baseH,nextH,lostHits,removed,baseStake,nextStake,baseRet,nextRet,baseRoi:+(baseRet/baseStake*100).toFixed(2),nextRoi:+(nextRet/nextStake*100).toFixed(2),profitDelta:(nextRet-nextStake)-(baseRet-baseStake)};}
const out={};for(const [name,arr] of Object.entries(ps)){out[name]={};for(const s of scores)for(const g of gaps)out[name][`s${s}_g${g}`]=test(arr,s,g);}console.log(JSON.stringify(out,null,2));
