'use strict';const assert=require('node:assert');const mod=require('../scripts/build-continuous-performance-ledger.cjs');const x=mod.build();assert.strictEqual(x.productionChanged,false);assert.strictEqual(x.policy.fixedTheoryABUnaffected,true);assert.ok(x.cumulative.races>=x.rolling100.races);assert.ok(x.rolling100.races<=100);assert.ok(x.cumulative.stakeYen>=0);assert.ok(x.cumulative.returnYen>=0);assert.ok(Array.isArray(x.generationBreakdown));assert.strictEqual(x.generationBreakdown.reduce((n,r)=>n+r.races,0),x.cumulative.races);console.log(JSON.stringify({cumulative:x.cumulative,rolling100:x.rolling100,generations:x.generationBreakdown.length},null,2));