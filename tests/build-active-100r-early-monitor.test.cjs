'use strict';const assert=require('node:assert');const {build}=require('../scripts/build-active-100r-early-monitor.cjs');const x=build();assert.strictEqual(x.productionChanged,false);assert.strictEqual(x.automaticProductionChange,false);const currentWindowCount=Number(x.reviewProgress?.currentWindowCount||0);assert.ok(Number.isFinite(currentWindowCount)&&currentWindowCount>=0);assert.ok(Number.isFinite(Number(x.scopedRaceCount))&&Number(x.scopedRaceCount)>=0);assert.ok(Number.isFinite(Number(x.matchedRows))&&Number(x.matchedRows)>=0);assert.ok(x.matchedRows<=x.scopedRaceCount);assert.ok(x.scopedRaceCount<=currentWindowCount);assert.strictEqual(x.duplicates,0);console.log(`active 100R early monitor passed: ${x.matchedRows}/100 settled, ${x.scopedRaceCount} scoped, ${currentWindowCount} eligible`);