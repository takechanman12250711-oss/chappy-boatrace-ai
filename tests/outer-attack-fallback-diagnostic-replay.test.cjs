'use strict';const assert=require('node:assert');const m=require('../scripts/build-outer-attack-fallback-diagnostic-replay.cjs');const r=m.build();assert.strictEqual(r.diagnosticOnly,true);assert.strictEqual(r.productionChanged,false);assert.strictEqual(r.automaticApplication,false);assert.ok(r.replayComparableRaceCount<=r.sourceNotComparableRaceCount);console.log(JSON.stringify({source:r.sourceNotComparableRaceCount,comparable:r.replayComparableRaceCount,variants:r.variants}));