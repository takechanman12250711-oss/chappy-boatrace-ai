'use strict';const assert=require('assert');const {build}=require('../scripts/test-outer-attack-course-cap.cjs');const r=build();assert.strictEqual(r.scope.holdoutUsed,false);assert.strictEqual(r.scope.productionChanged,false);assert.strictEqual(r.scope.baselineTop1,124);assert.strictEqual(r.candidates.length,5);assert.ok(r.candidates.every(x=>x.all.triggeredRaceCount===12));console.log('outer attack course cap tests passed');