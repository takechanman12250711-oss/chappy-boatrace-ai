'use strict';const assert=require('node:assert');const m=require('../scripts/build-outer-attack-gate-dropoff.cjs');const r=m.build();assert.strictEqual(r.productionChanged,false);assert.strictEqual(r.automaticApplication,false);assert.ok(r.counts.total>=r.counts.preDeadline);assert.strictEqual(r.rows.length,r.counts.preDeadline);assert.strictEqual(r.rows.filter(x=>['active','ambiguous'].includes(x.gate)).length,r.counts.active+r.counts.ambiguous);console.log(JSON.stringify(r.counts));