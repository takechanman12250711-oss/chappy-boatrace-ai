'use strict';const assert=require('node:assert');const cp=require('node:child_process');const x=JSON.parse(cp.execFileSync(process.execPath,['scripts/audit-workflow-overlap-health.cjs'],{encoding:'utf8'}));assert.strictEqual(x.productionChanged,false);assert.strictEqual(x.destructiveCleanupRecommended,false);assert.ok(x.workflowCount>0);assert.ok(x.known.centralResults);console.log('workflow overlap health audit passed');