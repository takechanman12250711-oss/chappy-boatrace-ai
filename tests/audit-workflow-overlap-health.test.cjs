'use strict';const assert=require('node:assert');const cp=require('node:child_process');const x=JSON.parse(cp.execFileSync(process.execPath,['scripts/audit-workflow-overlap-health.cjs'],{encoding:'utf8'}));assert.strictEqual(x.productionChanged,false);assert.strictEqual(x.destructiveCleanupRecommended,false);assert.ok(x.workflowCount>0);assert.ok(x.known.centralResults);assert.ok(x.known.wall?.workflowRuns.includes('Collect official race results'));assert.ok(x.findings.some(f=>f.code==='MULTI_TRIGGER_SINGLE_BUILDER'));console.log('workflow overlap health audit passed');