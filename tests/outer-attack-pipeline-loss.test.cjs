'use strict';const assert=require('node:assert');const m=require('../scripts/build-outer-attack-pipeline-loss.cjs');const r=m.build();assert.strictEqual(r.productionChanged,false);assert.strictEqual(r.automaticApplication,false);assert.strictEqual(r.rows.length,r.signalRaceCount);assert.strictEqual(Object.values(r.stageCounts).reduce((a,b)=>a+b,0),r.signalRaceCount);console.log(JSON.stringify({signalRaceCount:r.signalRaceCount,stageCounts:r.stageCounts,notComparableVariantStatus:r.notComparableVariantStatus}));