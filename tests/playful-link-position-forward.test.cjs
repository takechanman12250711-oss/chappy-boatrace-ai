'use strict';const assert=require('node:assert');const m=require('../scripts/build-playful-link-position-forward.cjs');const r=m.build();assert.strictEqual(r.productionChanged,false);assert.strictEqual(r.automaticApplication,false);assert.strictEqual(r.preregistration.discoveryDateExcluded,true);assert.ok(r.rows.every(x=>Date.parse(x.selectedAt)>Date.parse(r.preregistration.registeredAt)));assert.ok(r.rows.every(x=>x.candidate.length>=x.base.length));assert.ok(r.rows.every(x=>x.candidate.length<=x.base.length+3));console.log(JSON.stringify({rows:r.rows.length,delta:r.delta}));